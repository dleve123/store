<% if notice %>
<p id="notice"><%= notice %></p>
<% end %>

<h1>Your Freaking Awesome Store</h1>

<% @products.each do |product| %>
  <div class="entry">
    <%= image_tag(product.image_url) %>
    <h3><%= product.title %></h3>
    <%= sanitize(product.description) %>
    <div class="product_line">
      <span class="price"><%= product.price %></span>
    </div>
  </div>
<% end %>
